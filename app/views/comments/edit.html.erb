<div class="container mt-5 form-group">
 <%= form_tag url_for(action: 'update'), method: "PATCH" do %>
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">

      <%= label_tag 'Commentaire' %>
      <%= text_field_tag 'content', @comment.content, class:"form-control" %>
      <br /> <br />

      <%= submit_tag "Editier ce commentaire", class:"btn btn-primary"%>

    <% end %>
          <%= link_to "Supprimer", gossip_comment_path(params[:gossip_id], params[:id]), class:"btn btn-danger", method:"DELETE"%>

</div>

