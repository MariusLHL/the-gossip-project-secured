	  
		<% @gossip.comment.each do |t|%>

  <div class="card">
    <div class="card-body">
      <h5 class="card-title"><%= t.user.first_name %> <%= t.user.last_name%></h5>
      <p class="card-text"><%= t.content%></p>
			<% if t.user_id == current_user.id%>
      <p class="card-text"><small class="text-muted"><%= link_to "edit ce commentaire", edit_gossip_comment_url(@gossip.id, t.id)%>
</small></p>
<% end %>
    </div>
  </div>

<% end %>
